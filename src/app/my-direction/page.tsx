import Link from "next/link";
import { ArrowRight, BookOpen, Check, ChevronRight, CircleHelp, Landmark, Map, RefreshCcw, Route, Sparkles } from "lucide-react";
import { redirect } from "next/navigation";
import { ButtonLink } from "@/components/ui";
import { getExplorationState, getSessionState } from "@/services/profile";
import { getCourses, getField, getInstitutions, getPathways } from "@/services/catalog";

export const dynamic = "force-dynamic";
export const metadata = { title: "My direction" };

const steps = [
  { key: "direction", label: "Choose a direction", detail: "You are here" },
  { key: "understand", label: "Understand this direction", detail: "Your next step" },
  { key: "routes", label: "Compare routes", detail: "After that" },
  { key: "courses", label: "Explore courses", detail: "Later" },
  { key: "places", label: "Find places to study", detail: "Later" },
];

export default async function MyDirectionPage() {
  const exploration = await getExplorationState();
  if (!exploration) redirect("/profile");
  const [field, state] = await Promise.all([getField(exploration.directionSlug), getSessionState()]);
  if (!field || !state || state.status !== "completed") redirect("/profile");
  const [pathways, courses, institutions] = await Promise.all([
    getPathways({ stage: state.stage, fieldSlug: field.slug }),
    getCourses({ fieldSlug: field.slug }),
    getInstitutions({ fieldSlug: field.slug }),
  ]);
  const firstSteps = (field.whatPeopleDo ?? []).slice(0, 3);
  const usefulSubjects = (field.usefulSubjects ?? []).slice(0, 4);
  const skills = (field.skills ?? []).slice(0, 4);
  return <main className="min-h-[calc(100dvh-4rem)] bg-[#fbfcfa]"><div className="cb-container cb-page mx-auto max-w-[1180px]"><header className="flex flex-wrap items-start justify-between gap-5"><div><p className="cb-eyebrow">Your chosen direction</p><h1 className="mt-3 text-[clamp(2.3rem,5vw,4.5rem)] font-semibold leading-[1.02] tracking-[-.06em] text-[#092b25]">{field.name}</h1><p className="mt-4 max-w-2xl text-lg leading-relaxed text-ink-600">This is your focused space to understand this direction before deciding what to study or where to go.</p></div><form action="/api/exploration" method="post"><input type="hidden" name="clear" value="true" /><button type="submit" className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-[#d5e2dc] bg-white px-3.5 text-sm font-semibold text-ink-600 transition hover:border-[#9fcab8]">Change direction<RefreshCcw className="h-4 w-4" /></button></form></header><section className="mt-8 rounded-[1.75rem] border border-[#cfe8dc] bg-[#effaf5] p-6 sm:p-8"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm font-semibold text-[#28775f]">Your exploration journey</p><p className="mt-1 text-sm text-ink-500">Reflection complete · Direction selected</p></div><span className="rounded-full bg-white px-3 py-1.5 text-xs font-bold text-[#28775f]">1 of 5 steps</span></div><div className="mt-6 grid gap-2 sm:grid-cols-5">{steps.map((step, index) => <div key={step.key} className={`relative rounded-xl p-3 ${index === 0 ? "bg-[#287d65] text-white" : "bg-white/75 text-ink-500"}`}><div className="flex items-center gap-2"><span className={`grid h-6 w-6 place-items-center rounded-full text-xs font-bold ${index === 0 ? "bg-white text-[#287d65]" : "bg-[#e1eee8] text-ink-400"}`}>{index === 0 ? <Check className="h-3.5 w-3.5" /> : index + 1}</span><span className="text-xs font-semibold">{step.label}</span></div><p className={`mt-2 pl-8 text-[11px] ${index === 0 ? "text-white/75" : "text-ink-400"}`}>{step.detail}</p></div>)}</div></section><section className="mt-8 grid gap-6 lg:grid-cols-[1.25fr_.75fr]"><div className="rounded-[1.5rem] border border-[#dce9e4] bg-white p-6 shadow-[0_12px_30px_-26px_rgba(12,57,44,.45)] sm:p-8"><div className="flex items-start gap-4"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-[#e9f6f0] text-[#287d65]"><Sparkles className="h-6 w-6" /></span><div><p className="cb-eyebrow">Step 1 · Understand it first</p><h2 className="mt-2 text-3xl font-semibold tracking-[-.04em] text-[#092b25]">What is {field.name.toLowerCase()}?</h2><p className="mt-3 text-base leading-relaxed text-ink-600">{field.overview}</p></div></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl bg-[#f6fbf8] p-4"><p className="text-xs font-bold uppercase tracking-[.08em] text-[#28775f]">What people may do</p><ul className="mt-3 space-y-2 text-sm leading-relaxed text-ink-600">{firstSteps.length ? firstSteps.map((item) => <li key={item} className="flex gap-2"><span className="text-[#287d65]">•</span>{item}</li>) : <li>Explore roles, study routes and real examples in this direction.</li>}</ul></div><div className="rounded-2xl bg-[#f8f5ff] p-4"><p className="text-xs font-bold uppercase tracking-[.08em] text-[#735bd1]">Useful preparation</p><p className="mt-3 text-sm font-semibold text-ink-700">Subjects</p><p className="mt-1 text-sm text-ink-500">{usefulSubjects.length ? usefulSubjects.join(", ") : "Depends on the route you choose"}</p><p className="mt-3 text-sm font-semibold text-ink-700">Skills</p><p className="mt-1 text-sm text-ink-500">{skills.length ? skills.join(", ") : "Skills grow through practice and study"}</p></div></div><div className="mt-7 flex flex-wrap gap-3"><ButtonLink href={`/pathways?field=${field.slug}`}>Compare routes<ArrowRight className="h-4 w-4" /></ButtonLink><Link href={`/explore/${field.slug}`} className="inline-flex min-h-11 items-center gap-2 rounded-xl px-3 text-sm font-semibold text-[#6043bd] underline decoration-[#baa9ef] underline-offset-4">Read the full direction guide<ChevronRight className="h-4 w-4" /></Link></div></div><aside className="space-y-4"><div className="rounded-[1.5rem] border border-[#dce9e4] bg-[#f7fbf9] p-6"><p className="cb-eyebrow">Based on your reflection</p><p className="mt-3 text-sm leading-relaxed text-ink-600">You chose this because it connects with the interests, strengths, and practical preferences you shared. You can change direction whenever you learn something new about yourself.</p><Link href="/profile" className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-[#28775f] underline decoration-[#9bcdb9] underline-offset-4">Review my starting point<ArrowRight className="h-4 w-4" /></Link></div><div className="rounded-[1.5rem] border border-[#d2e2f4] bg-[#eff7ff] p-6"><div className="flex gap-3"><CircleHelp className="h-5 w-5 shrink-0 text-[#2877df]" /><p className="text-sm leading-relaxed text-ink-600">You are exploring a direction, not making a final career choice. Compare more than one route before deciding.</p></div></div></aside></section><section className="mt-8"><p className="cb-eyebrow">What becomes available next</p><h2 className="mt-2 text-2xl font-semibold tracking-[-.03em] text-[#092b25]">Your direction, in practical steps</h2><div className="mt-5 grid gap-4 md:grid-cols-3"><Link href={`/pathways?field=${field.slug}`} className="group rounded-2xl border border-[#dce9e4] bg-white p-5 transition hover:-translate-y-0.5 hover:border-[#9ccfbc]"><Route className="h-5 w-5 text-[#287d65]" /><p className="mt-4 font-semibold text-[#123f38]">Compare routes</p><p className="mt-1 text-sm leading-relaxed text-ink-500">{pathways.length || 0} route{pathways.length === 1 ? "" : "s"} connected to this direction.</p><span className="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-[#287d65]">Open next step<ArrowRight className="h-4 w-4" /></span></Link><Link href={`/courses?field=${field.slug}`} className="group rounded-2xl border border-[#e0e8f2] bg-white p-5 opacity-75 transition hover:opacity-100"><BookOpen className="h-5 w-5 text-[#4e78ad]" /><p className="mt-4 font-semibold text-[#123f38]">Explore courses</p><p className="mt-1 text-sm leading-relaxed text-ink-500">{courses.length || 0} course option{courses.length === 1 ? "" : "s"} to look at after comparing routes.</p><span className="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-[#4e78ad]">Preview later<ArrowRight className="h-4 w-4" /></span></Link><Link href={`/institutions?field=${field.slug}`} className="group rounded-2xl border border-[#e0e8f2] bg-white p-5 opacity-75 transition hover:opacity-100"><Landmark className="h-5 w-5 text-[#4e78ad]" /><p className="mt-4 font-semibold text-[#123f38]">Find places to study</p><p className="mt-1 text-sm leading-relaxed text-ink-500">{institutions.length || 0} institution{institutions.length === 1 ? "" : "s"} connected to this direction.</p><span className="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-[#4e78ad]">Preview later<ArrowRight className="h-4 w-4" /></span></Link></div></section><div className="mt-8 flex items-start gap-3 rounded-2xl border border-[#d2e2f4] bg-[#eff7ff] px-5 py-4"><Map className="mt-0.5 h-5 w-5 shrink-0 text-[#2877df]" /><p className="text-sm leading-relaxed text-ink-600"><span className="font-semibold text-[#194b71]">Your progress is saved on this device.</span> We will keep this direction as your current starting point while you explore.</p></div></div></main>;
}
